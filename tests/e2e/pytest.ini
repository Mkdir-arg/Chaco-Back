[pytest]
# Config del harness E2E. El navegador visible / cámara lenta se pasan por CLI:
#   --headed --slowmo 800
# Evidencia opcional en cada corrida:
#   --screenshot only-on-failure --video retain-on-failure --tracing retain-on-failure
addopts = -v -ra
