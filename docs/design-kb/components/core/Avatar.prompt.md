**Avatar** — circular user marker; renders gradient initials when no image is given (the navbar avatar pattern).

```jsx
<Avatar name="María González" size={40} />
<Avatar src="/uploads/foto.jpg" size={48} />
```

Props: `name`, `src`, `size` (px), `square`.
