# Registro técnico — Issues #239 a #251

Fecha de inicio: 18/08/2026

Este documento registra los cambios realizados en los dos repositorios involucrados para facilitar auditoría, diagnóstico y una eventual reversión. No reemplaza a los Issues de GitHub como fuente funcional.

## Repositorios y estrategia

| Superficie | Repositorio | Rama base / destino | Forma de trabajo |
|---|---|---|---|
| Backend y backoffice | `Mkdir-arg/Chaco` | `development` | Rama `feat/issues-239-251-mobile-relevamientos`, luego PR a `development` |
| App móvil | `Mkdir-arg/Chaco-mobile` (directorio `mobile/`) | `main` | Commits directos a `main` |

## Estado inicial comprobado

- La rama `feat/issues-239-251-mobile-relevamientos` estaba sincronizada con `origin/development`.
- El repositorio anidado `mobile/` estaba sincronizado con `origin/main` después del commit indicado abajo.
- `mobile/` tiene su propio directorio `.git`; no debe operarse como una carpeta del repositorio principal.

## Cambio móvil previo — servidor y consulta de personas

Commit publicado en `Mkdir-arg/Chaco-mobile/main`:

```text
6c1bc38 fix: actualizar servidor y consulta de personas
```

Cambios:

- `app.json`: API base de `https://datanach.ecomdev.ar` a `https://relevamiento-deshum.ecomdev.ar`.
- `plugins/withAndroidNetworkSecurityConfig.js`: dominio TLS actualizado.
- `src/screens/RelevamientoDetailScreen.js`: consulta de identidad de `/api/becas/renaper/consultar/` a `/api/becas/personas/consultar/`.
- `README.md`: documentación alineada con servidor y endpoint nuevos.
- Se eliminaron artefactos locales no versionados: `Relevamiento-Chaco-Test.apk` y `dist-test-server/`.

Validación:

- `git diff --check`: correcto.
- `expo-doctor`: 17/18 controles; deuda previa de versiones patch en `expo`, `expo-constants` y `expo-file-system`.

Reversión, si fuera necesaria:

```powershell
git -C mobile revert 6c1bc38
git -C mobile push origin main
```

Usar `revert`, no reescribir la historia de `main`.

## Implementación de Issues #239–#251

### #239 — Etiquetas de usuarios

Estado: implementado y validado.

Cambios realizados hasta ahora en el repositorio principal:

- Nuevo helper `users/presentation.py::etiqueta_usuario`.
- Formato: `Nombre Apellido (DNI)`; sin DNI, solo nombre; sin nombre, `username`; tolera usuarios sin `Profile`.
- Aplicación iniciada en los selectores de coordinador, referente y territorial de `programas/forms.py`.
- El queryset principal de territoriales incorpora `select_related("profile")` para evitar N+1.

Reversión antes de crear el commit:

```powershell
git restore -- programas/forms.py
Remove-Item -LiteralPath users/presentation.py
```

Antes de ejecutar esa reversión se debe comprobar que esos archivos no contengan cambios posteriores que se quieran conservar.

### #240 — Backoffice con fecha y hora

Estado: implementado y validado.

- `Relevamiento.fecha_asignada` y `fecha_hasta` pasan a `DateTimeField`.
- Alta y reprogramación usan controles `datetime-local`.
- La validación contra convocatoria conserva comparación por día.
- `esta_vencido` compara contra `timezone.now()`.
- Migración `0046_relevamiento_franja_horaria.py`: conserva el día histórico como 00:00:00–23:59:59 en la zona local.

### #241 — API datetime y vigencia actual

Estado: implementado y validado.

- El listado filtra `fecha_asignada <= ahora <= fecha_hasta`.
- Las operaciones usan el instante real o `capturado_en` para conservar sync offline válido.
- El serializer expone ISO 8601 con zona horaria.

### #242 — App: franja y vigencia por hora

Estado: publicado en `Chaco-mobile/main`, commit `a6187d1`.

- Lista y detalle muestran fecha y hora.
- La app habilita operaciones únicamente dentro de la franja datetime.
- La caché local también se filtra por instante vigente.

### #248 — App: recuperación de contraseña

Estado: app publicada en `Chaco-mobile/main` (`a6187d1`); flujo web incluido en la rama del repositorio principal.

- Botón móvil abre `/recuperar-contrasena/` sobre el host configurado.
- Se agregó el flujo web dependiente: solicitud neutral, correo tokenizado y confirmación existente.

### #249 — App: omitir preguntas vacías

Estado: publicado en `Chaco-mobile/main`, commit `a6187d1`.

- El stepper se deriva de preguntas activas de globales, programa, segmento y subsegmento.
- Siguiente/anterior recorren la lista efectiva y el total omite secciones vacías.
- Sin preguntas dinámicas, un adulto pasa de Persona a Confirmación; contacto se completa allí.
- Para menores se conserva la pantalla necesaria para datos del apoderado.

### #251 — App: botón ojo

Estado: publicado en `Chaco-mobile/main`, commit `a6187d1`.

- El ojo existente ahora alterna label accesible Mostrar/Ocultar contraseña y conserva default oculto al montar el login.

## Validaciones obligatorias previstas

Repositorio principal:

- Tests dirigidos de modelos, relevamientos, API y formularios de usuarios.
- `manage.py check` con `.venv`.
- `makemigrations --check`.
- Por cambios de templates/UI: `check_design_agent.py --changed`, `design_audit.py` y `compile_templates.py`.

Repositorio móvil:

- `git diff --check`.
- Validación Expo disponible.
- Revisión manual de navegación, accesibilidad del login, fechas y conservación offline.

## Historial de commits de esta tanda

| Repositorio | Commit | Alcance | Publicado |
|---|---|---|---|
| `Chaco-mobile` | `6c1bc38` | Servidor de prueba y endpoint Base de Personas | Sí, `main` |
| `Chaco-mobile` | `a6187d1` | Issues #242, #248, #249 y #251 | Sí, `main` |

Agregar aquí cada commit nuevo antes de publicarlo.

## Validaciones ejecutadas

- `manage.py check`: OK.
- `makemigrations --check --dry-run`: sin cambios pendientes.
- 10 tests dirigidos de #239–#241: OK.
- Test del flujo neutral de recuperación: OK.
- `check_design_agent.py --changed`: OK.
- `design_audit.py` sobre templates tocados: 0 errores, 0 warnings.
- `compile_templates.py`: 160 templates, 0 errores.
- `git diff --check` en ambos repositorios: OK.
- Dos compilaciones `expo export --platform web`: OK; artefactos temporales eliminados.
- La suite histórica completa de relevamientos no pudo concluir dentro del límite externo de 120 segundos; las ejecuciones huérfanas se limpiaron reiniciando únicamente el contenedor `app`. Las pruebas dirigidas posteriores finalizaron correctamente.

## Incidencias del entorno local

- No había `.venv` al iniciar.
- Python 3.12 no está instalado; se creó `.venv` con Python 3.14, soportado por Django 5.2.
- `pip install -r requirements.txt` no pudo compilar `mysqlclient==2.1.1` por falta de Microsoft Visual C++ 14+ y ausencia de wheel para Python 3.14.
- Se decidió usar el servicio Docker `app`, ya existente y saludable, para migraciones y tests. No se alteraron dependencias ni se instaló un toolchain global.
